#include "main.h"

/**
 * _abs - a function that returns absolute value of the number
 * @n: input for finding absolute value
 * Return: always 1 or 0
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return ((-1) * n);

}

